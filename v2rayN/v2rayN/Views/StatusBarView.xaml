<reactiveui:ReactiveUserControl
    x:Class="v2rayN.Views.StatusBarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:reactiveui="http://reactiveui.net"
    xmlns:resx="clr-namespace:ServiceLib.Resx;assembly=ServiceLib"
    xmlns:tb="clr-namespace:H.NotifyIcon;assembly=H.NotifyIcon.Wpf"
    xmlns:vms="clr-namespace:ServiceLib.ViewModels;assembly=ServiceLib"
    d:DesignHeight="450"
    d:DesignWidth="800"
    x:TypeArguments="vms:StatusBarViewModel"
    Style="{StaticResource ViewGlobal}"
    mc:Ignorable="d">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="34" />
        </Grid.RowDefinitions>
        <Border BorderBrush="{StaticResource QccBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="18,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.05*" />
                    <ColumnDefinition Width="0.95*" />
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="1.15*" />
                    <ColumnDefinition Width="150" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Padding="0,0,20,0" BorderBrush="{StaticResource QccBorder}" BorderThickness="0,0,1,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="{StaticResource QccText}" FontSize="13" FontWeight="SemiBold" Text="系统代理" />
                        <ComboBox
                            x:Name="cmbSystemProxy"
                            Height="36"
                            Margin="0,7,0,0"
                            Padding="10,3"
                            materialDesign:HintAssist.Hint="{x:Static resx:ResUI.menuSystemproxy}"
                            AutomationProperties.Name="{x:Static resx:ResUI.menuSystemproxy}"
                            FontSize="13"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}">
                            <ComboBoxItem Content="{x:Static resx:ResUI.menuSystemProxyClear}" />
                            <ComboBoxItem Content="{x:Static resx:ResUI.menuSystemProxySet}" />
                            <ComboBoxItem Content="{x:Static resx:ResUI.menuSystemProxyNothing}" />
                            <ComboBoxItem Content="{x:Static resx:ResUI.menuSystemProxyPac}" />
                        </ComboBox>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Padding="20,0" BorderBrush="{StaticResource QccBorder}" BorderThickness="0,0,1,0">
                    <StackPanel x:Name="spEnableTun" VerticalAlignment="Center">
                        <TextBlock Foreground="{StaticResource QccText}" FontSize="13" FontWeight="SemiBold" Text="TUN 模式" />
                        <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                            <ToggleButton
                                x:Name="togEnableTun"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Center"
                                AutomationProperties.Name="{x:Static resx:ResUI.TbEnableTunAs}" />
                            <TextBlock Margin="10,0,0,0" VerticalAlignment="Center" Foreground="{StaticResource QccMuted}" FontSize="12" Text="{x:Static resx:ResUI.TbEnableTunAs}" />
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Padding="20,0" BorderBrush="{StaticResource QccBorder}" BorderThickness="0,0,1,0">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Foreground="{StaticResource QccText}" FontSize="13" FontWeight="SemiBold" Text="路由模式" />
                        <ComboBox
                            x:Name="cmbRoutings2"
                            Height="36"
                            Margin="0,7,0,0"
                            Padding="10,3"
                            materialDesign:HintAssist.Hint="{x:Static resx:ResUI.menuRouting}"
                            AutomationProperties.Name="{x:Static resx:ResUI.menuRouting}"
                            DisplayMemberPath="Remarks"
                            FontSize="13"
                            Style="{StaticResource MaterialDesignOutlinedComboBox}" />
                    </StackPanel>
                </Border>

                <StackPanel Grid.Column="3" Margin="20,0,0,0" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource QccText}" FontSize="13" FontWeight="SemiBold" Text="端口状态" />
                    <Grid Margin="0,7,0,0">
                        <Grid.RowDefinitions><RowDefinition /><RowDefinition /></Grid.RowDefinitions>
                        <TextBlock x:Name="txtInboundDisplay" Foreground="{StaticResource QccMuted}" FontSize="12" TextTrimming="CharacterEllipsis" />
                        <TextBlock x:Name="txtInboundLanDisplay" Grid.Row="1" Margin="0,7,0,0" Foreground="{StaticResource QccMuted}" FontSize="12" TextTrimming="CharacterEllipsis" />
                    </Grid>
                </StackPanel>

                <Border Grid.Column="4" Margin="18,0,0,0" Padding="18,0,0,0" BorderBrush="{StaticResource QccBorder}" BorderThickness="1,0,0,0">
                    <Button x:Name="btnAdvancedSettings" VerticalAlignment="Center" Style="{StaticResource QccSecondaryButton}" Click="AdvancedSettings_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Width="18" Height="18" Margin="0,0,8,0" Kind="CogOutline" />
                            <TextBlock VerticalAlignment="Center" Text="高级设置" />
                            <materialDesign:PackIcon Width="17" Height="17" Margin="8,0,0,0" Kind="ChevronRight" />
                        </StackPanel>
                    </Button>
                </Border>

                <StackPanel Visibility="Collapsed">
                    <TextBlock x:Name="txtSpeedProxyDisplay" />
                    <TextBlock x:Name="txtSpeedDirectDisplay" />
                    <TextBlock x:Name="txtRunningServerDisplay" />
                    <TextBlock x:Name="txtRunningInfoDisplay" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="#FFFBFCFE" BorderBrush="{StaticResource QccBorder}" BorderThickness="0,1,0,0">
            <Grid Margin="18,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1.25*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1.1*" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <Ellipse Width="8" Height="8" Margin="0,0,8,0" Fill="{StaticResource QccMuted}" />
                    <TextBlock x:Name="txtBottomRunningStatus" Foreground="{StaticResource QccMuted}" FontSize="12" Text="未连接" TextTrimming="CharacterEllipsis" />
                </StackPanel>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="{StaticResource QccMuted}" FontSize="12" Text="{Binding InboundDisplay, StringFormat=本地：{0}, FallbackValue=本地：—}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="2" VerticalAlignment="Center" Foreground="{StaticResource QccMuted}" FontSize="12" Text="{Binding SelectedRouting.Remarks, StringFormat=路由：{0}, FallbackValue=路由：—}" TextTrimming="CharacterEllipsis" />
                <TextBlock Grid.Column="3" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{StaticResource QccMuted}" FontSize="12" Text="系统代理 / TUN 状态实时同步" />
            </Grid>
        </Border>

        <tb:TaskbarIcon
            x:Name="tbNotify"
            IconSource="/Resources/v2rayN.ico"
            NoLeftClickDelay="True"
            ToolTipText="v2rayN">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu Style="{StaticResource DefContextMenu}">
                    <MenuItem
                        x:Name="menuSystemProxyClear"
                        Height="{StaticResource MenuItemHeight}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuSystemProxyClear}">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    x:Name="menuSystemProxyClear2"
                                    Margin="{StaticResource MarginRight8}"
                                    VerticalAlignment="Center"
                                    Kind="Check" />
                                <TextBlock Text="{x:Static resx:ResUI.menuSystemProxyClear}" />
                            </StackPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <MenuItem
                        x:Name="menuSystemProxySet"
                        Height="{StaticResource MenuItemHeight}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuSystemProxySet}">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    x:Name="menuSystemProxySet2"
                                    Margin="{StaticResource MarginRight8}"
                                    VerticalAlignment="Center"
                                    Kind="Check" />
                                <TextBlock Text="{x:Static resx:ResUI.menuSystemProxySet}" />
                            </StackPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <MenuItem
                        x:Name="menuSystemProxyNothing"
                        Height="{StaticResource MenuItemHeight}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuSystemProxyNothing}">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    x:Name="menuSystemProxyNothing2"
                                    Margin="{StaticResource MarginRight8}"
                                    VerticalAlignment="Center"
                                    Kind="Check" />
                                <TextBlock Text="{x:Static resx:ResUI.menuSystemProxyNothing}" />
                            </StackPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <MenuItem
                        x:Name="menuSystemProxyPac"
                        Height="{StaticResource MenuItemHeight}"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuSystemProxyPac}">
                        <MenuItem.Header>
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon
                                    x:Name="menuSystemProxyPac2"
                                    Margin="{StaticResource MarginRight8}"
                                    VerticalAlignment="Center"
                                    Kind="Check" />
                                <TextBlock Text="{x:Static resx:ResUI.menuSystemProxyPac}" />
                            </StackPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <Separator x:Name="sepRoutings" />
                    <MenuItem
                        x:Name="menuRoutings"
                        Height="Auto"
                        AutomationProperties.Name="{x:Static resx:ResUI.menuRouting}">
                        <MenuItem.Header>
                            <DockPanel>
                                <ComboBox
                                    x:Name="cmbRoutings"
                                    MaxWidth="300"
                                    materialDesign:HintAssist.Hint="{x:Static resx:ResUI.menuRouting}"
                                    AutomationProperties.Name="{x:Static resx:ResUI.menuRouting}"
                                    DisplayMemberPath="Remarks"
                                    FontSize="{DynamicResource StdFontSize}"
                                    Style="{StaticResource MaterialDesignFilledComboBox}" />
                            </DockPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <MenuItem Height="Auto" AutomationProperties.Name="{x:Static resx:ResUI.menuServers}">
                        <MenuItem.Header>
                            <DockPanel>
                                <ComboBox
                                    x:Name="cmbServers"
                                    MaxWidth="300"
                                    materialDesign:HintAssist.Hint="{x:Static resx:ResUI.menuServers}"
                                    AutomationProperties.Name="{x:Static resx:ResUI.menuServers}"
                                    DisplayMemberPath="Text"
                                    FontSize="{DynamicResource StdFontSize}"
                                    Style="{StaticResource MaterialDesignFilledComboBox}" />
                            </DockPanel>
                        </MenuItem.Header>
                    </MenuItem>
                    <Separator />
                    <MenuItem
                        x:Name="menuAddServerViaClipboard2"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuAddServerViaClipboard}" />
                    <MenuItem
                        x:Name="menuAddServerViaScan2"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuAddServerViaScan}" />
                    <MenuItem
                        x:Name="menuSubUpdate2"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuSubUpdate}" />
                    <MenuItem
                        x:Name="menuSubUpdateViaProxy2"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuSubUpdateViaProxy}" />
                    <Separator />
                    <MenuItem
                        x:Name="menuCopyProxyCmdToClipboard"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuCopyProxyCmdToClipboard}" />
                    <Separator />
                    <MenuItem
                        x:Name="menuExit"
                        Height="{StaticResource MenuItemHeight}"
                        Header="{x:Static resx:ResUI.menuExit}" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>
    </Grid>
</reactiveui:ReactiveUserControl>
